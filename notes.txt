Use dependency injection to test functions independently.

Focus on correctness of logic without relying on real system calls.

go run main.go -metric=cpu -seconds=5

big development:
disk.Usage("/") → Gets disk space metrics (total, used, free) 
based on the mount point.

disk.IOCounters("sda") → Gets I/O stats (read/write activity) 
for the physical device.